services:
  - type: web
    name: scamshield-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn scamshield_agent_integration:app --host 0.0.0.0 --port $PORT
    plan: free
